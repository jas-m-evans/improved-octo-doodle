# Sample data

No binary sample clip is committed to keep the MVP small.

Use the built-in analysis self-check to generate confidence that the analysis path works:

```bash
cd drum_coach_mvp
source .venv/bin/activate
python analysis.py --self-check
```

Then upload any short phone recording of drum practice to test the full Streamlit flow.
